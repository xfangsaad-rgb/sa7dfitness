{
  "name": "sa7dfitness",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:identity",
    "build:identity": "esbuild src/identity-client.entry.js --bundle --format=iife --platform=browser --target=es2020 --global-name=SA7DIdentityModule --outfile=identity-client.js",
    "preview:local": "npm run build && npx firebase-tools emulators:start --only hosting --project sa7dfitness-b0de3",
    "deploy:preview": "npm run build && npx firebase-tools hosting:channel:deploy preview --project sa7dfitness-b0de3",
    "deploy:live": "npm run build && npx firebase-tools deploy --only hosting --project sa7dfitness-b0de3",
    "android:sync": "npx cap sync android",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@capacitor/local-notifications": "^8.0.2",
    "@netlify/blobs": "^8.1.0",
    "@netlify/identity": "^0.4.0",
    "firebase": "^12.12.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.11",
    "firebase-tools": "^15.16.0"
  },
  "engines": {
    "node": ">=22"
  }
}
