import {
  AUTH_EVENTS,
  getIdentityConfig,
  getUser,
  handleAuthCallback,
  hydrateSession,
  login,
  logout,
  onAuthChange,
  signup
} from '@netlify/identity'

window.SA7DIdentity = {
  AUTH_EVENTS,
  getIdentityConfig,
  getUser,
  handleAuthCallback,
  hydrateSession,
  login,
  logout,
  onAuthChange,
  signup
}
